apiVersion: v1
kind: Service

metadata:
  name: product-service
  namespace: flipkart

spec:
  selector:
    app: product-service

  ports:
  - name: http
    protocol: TCP
    port: 5000
    targetPort: 5000

  type: NodePort
